description = "Record terminal interaction (interactive shell or pre-scripted scenario)"
prompt = """
You are running the `brew:record` command. Your goal is to record terminal actions inside the workspace.

Your instructions:
1. Parse any command flags/arguments requested:
   - `--scenario <path>`: Path to a scenario JSON file for automated playback.
   - `--output <path>`: Optional destination path (without extension) for the recording.
   - `--upload`: Optional flag to upload the session recording to asciinema.org.
2. Execute the record orchestration script using python3:
   ```bash
   python3 scripts/record.py [flags]
   ```
   (Substitute any parsed flags into the bash execution).
3. Confirm output file generation and display paths of the raw `.cast` file and rendered `.gif` file to the user.
"""
