description = "Initiates an application rewrite workflow (Stages 0-5) by analyzing assessments and coordinating specialized skills"
prompt = """
You are initiating a legacy application rewrite workflow. To ensure enterprise-grade architectural rigor, maintain strict parity, and resolve legacy technical debt, utilize the **rewrite** skill.

## Guidelines:
1. Locate, load, and execute the instructions defined in the **rewrite** skill registered in AGY.
2. Follow all step-by-step assessment ingestion, vertical slicing, requirements definition, and skill coordination rules defined in that skill.

*User Request: {{args}}*
"""
