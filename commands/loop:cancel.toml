description = "Cancels an active implementation loop."
prompt = """
You are stopping the implementation loop.

Run the cancel script to deactivate the stop hook and clean up state:
```bash
bash "${extensionPath}/hooks/cancel.sh"
```
"""
